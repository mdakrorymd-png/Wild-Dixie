fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.1
pydantic[email]==2.10.4
PyJWT==2.10.1
bcrypt==4.2.1
python-multipart==0.0.20
httpx==0.28.1
# playwright is optional (browser fallback for the importer). Omitted from the
# serverless deploy — the HTTP-first scrape needs no browser. `pip install
# playwright playwright-stealth` locally if you want the fallback.
